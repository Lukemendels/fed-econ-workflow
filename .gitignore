# Claude Code local files (not shared across machines)
.claude/settings.local.json
.claude/state/
CLAUDE.local.md

# Agency-specific working notes that must never be committed — see
# controlled-information.md's "This rule must stay generic" section.
local/

# OS files
.DS_Store
Thumbs.db

# Editor
.vscode/*
!.vscode/settings.json
.idea/
*.swp
*.swo
*~

# Node (build-chat.mjs has no dependencies today, but keep this ready)
node_modules/
